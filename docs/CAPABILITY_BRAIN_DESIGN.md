# Capability + shared-brain architecture (cbox)

Status: GREENLIT for M1 - passed a final alien fable/max adversarial review (GREENLIGHT-WITH-CONDITIONS, 30+ line anchors sample-verified against the live tree, no redesign required) after two codex-sol WITHHOLD rounds were incorporated. The alien conditions (F1-F8) are folded in below. Prior history: codex-sol WITHHELD twice over 2 CRITICAL items (mode_pin depended on the claude hook; a user stdio entry can bypass shim-wrap) plus an M3 proof gap and several false code anchors; every sol finding was adversarially re-verified against the actual code (all CONFIRMED or PARTIAL, none refuted). Incorporated fixes: mode_pin reclassified as a b2 claude-only behavioral guard; M3 gets a frozen pre-extraction oracle instead of a digest-equality claim; false anchors corrected in place; the bounded-core-edit inventory is the FULL audited set with a projection-aware CI check; the enabled_when_env compound residual names the conditional hermes-local case as an explicit non-closed residual; two overstated wording claims corrected (the manifest proves generated-artifact parity on disk, not runtime receipt; the red line is LARGELY satisfied with documented exceptions). SHIM-WRAP RECLASSIFIED (alien F1 + Marek 08-08 container-boundary ruling): the shim-wrap argv-resolution escape is NOT a live b1 security gap and there is no pending render_mcp.py fix - the cbox CONTAINER is the security boundary (an unshimmed codex inside it cannot mount anything extra, has no docker socket, and danger-full-access is the intended in-container default), so shim-wrap is P2 FUNCTIONAL INTEGRITY (tier pin + depth + kernel injection) plus defense-in-depth, not a broken security invariant. Author: alien fable/max (Marek's request), from the empirical engine-parity terrain surfaced by the DEVEL-mode work. Norm siblings: CLI_UNIFICATION_DESIGN.md, USER_EXTENSION_DESIGN.md. Next: M1 implementation (this greenlight).

STATUS-CORRECTION (post-implementation audit, 2026-08-09): The landed M3 adapters (etc/adapters/{claude,codex,hermes}.py) are CLI-verb serializers (cmd_json_seed, cmd_switch_flag_merge, and similar) for artifact generation (json-seed, hooks-json, mcp-servers-yaml), NOT the section-6 ADAPTER CONTRACT functions (describe, render_context, render_tools, render_guards) - none of those were implemented. The fourth-engine admission goal is carried by the assertion-5 projection-aware CI inventory (lib/test_capabilities_registry.sh assertion 5) plus the M6 entrypoint-arm gap pins, not by an encapsulated adapter contract. Because render_guards()'s DECLINE-with-reason feed was never built, the per-engine guard census was hand-written and could drift - the phantom-binding finding (fixed separately in test_capabilities_registry.sh assertion 6) discovered this. A bindings-to-delivery test now guards that class: every capability binding whose artifact is a *_guard_bridge.py must reference its guard's source script inside the bridge.

AUDIT (2026-08-09) - SHIP POSTURE: The global three-engine path (claude/codex/hermes) is architecturally sound per the plane placement rules and red-line placement. Isolated-hermes managed MCP/hooks (the dual-admin pattern for hermes in contained environments, once planned for M2) remains incomplete and unshipped. M5 brain-write mining (backfilling PROGRESS/CHANGELOG from native history for engines without w2 hooks) is manual and internal (not lifecycle-wired into the renderers). M4 guard experiments (codex PreToolUse + hermes shell hooks) deliver rm-glob and commit-guard coverage only; code-hygiene cannot fire on codex (file edits bypass Bash-only interception) and experimental status means no claude-equivalence claim. The capability manifest is documentary - it proves that declared capabilities and their bindings match what was generated on disk (artifact parity), not that guards execute, hooks fire, or runtime delivery occurs.

ENGINE-AGNOSTIC CAPABILITY + SHARED BRAIN ARCHITECTURE - design (deliverable for Marek)

DESIGN SUMMARY
One brain, one registry, N thin adapters, one proof. The unit of the system is a CAPABILITY: a declared cbox feature with an enforcement class, a payload source, and a per-engine delivery binding. Capabilities are declared once in a new `etc/capabilities/capabilities.json` (sibling of the three registries that already exist: `etc/registry/settings.json` knobs, `etc/engines/engines.json` engine facts, `etc/mcp/delegates.json` tools). Engines never get features coded ad-hoc; they get adapters - thin per-engine transports with a fixed contract - and the parity between what the registry declares and what each engine's GENERATED ARTIFACTS carry is proved by extending the existing context-manifest digest mechanism (generators.sh:2820-2900, doctor row "context-manifest"). Precise claim (sol re-review): the digest proves generated-artifact parity on disk - it does NOT by itself prove installed mounts, hook execution, or runtime receipt by a live engine; those remain verified by the boot preflights and doctor rows, not by the manifest. The shared brain is NOT one capability among others: it is the data layer underneath, engine-agnostic by construction (files on disk), and capabilities are the delivery/enforcement layer that gives any client access to it. The red line holds by placement, not promise: a HARD security invariant is only ever enforced in the two engine-blind planes (substrate and mediated tools), so an engine that lacks hooks can never silently lose a guard - it either never had the dangerous ability, or the dependent feature is off with a doctor row. This is buildable because the tree already contains every load-bearing precedent: the shim already enforces guards server-side for any caller and injects the kernel (codex_mcp_shim.py:412-450), one loader already serves two engines' brain injection (continuity_session_start.py via claude SessionStart and codex hooks.json), and the manifest digest idiom already proves context parity.

CORRECTION TO THE INPUT SCAN (empirical, changes the matrix): both non-claude engines have grown upstream hook systems since the 07-31 ledger note. Codex CLI ships an experimental hooks engine (v0.114+, behind `[features].codex_hooks = true`): PreToolUse/PostToolUse exist but PreToolUse intercepts ONLY the Bash tool (no file-write, no MCP-call interception), can only deny, and PostToolUse is observe-only (corroborated by openai/codex issues #18491 and #14754 - both are open feature requests to extend beyond Bash). Hermes Agent ships shell hooks declared in config.yaml `hooks:` with claude-like shape (matcher/command/timeout, JSON payload on stdin with hook_event_name/tool_name/tool_input/session_id/cwd), events mirroring plugin events including `pre_tool_call` (CAN block via stdout JSON `{"decision":"block","reason":...}`), `post_tool_call`, `pre_llm_call`, session lifecycle - with two sharp caveats: non-zero exit and malformed JSON DO NOT block (fail-open on crash), and the first-use consent allowlist keys on the command STRING, not the script hash (editing the script does not invalidate consent). All of this is third-party/upstream documentation: whether the PINNED versions in cbox (CBOX_CODEX_VERSION, CBOX_HERMES_VERSION) carry these features and with exactly these semantics is a host experiment, and both guard increments below stay gated behind it with bump tripwires. The honest problem statement is therefore no longer "hermes has nothing, physics forbids it" - it is "three hook dialects with different coverage, blocking semantics, crash behavior, and trust models, plus future clients that may have nothing." That is exactly the problem a registry + adapters + proof architecture is for, and it is NOT a problem one mechanism can solve.

1. THE THREE PLANES - what is physically engine-agnostic and what is not

Every cbox delivery mechanism in the tree today lands in one of three planes. The planes are the physical truth of the design; capabilities declare which plane(s) they use, and the red line is a placement rule over them.

P1 SUBSTRATE (engine-blind, works for any client including future ones, because it needs zero engine cooperation):
- container boundary + mounts: `:ro` binds for hooks/agents/managed-settings (generators.sh mounts region), user layer `:ro`, egress proxy/SOCKS + allowlists, no docker socket;
- entrypoint argv pinning: codex always runs `--strict-config --profile cbox-container --dangerously-bypass-hook-trust` and rejects `-p/--profile` overrides (entrypoint.sh:635-645); hermes always gets the composed ephemeral system prompt (entrypoint.sh:704-710);
- boot gates: claude refuses to start if any codex-* NAMED MCP entry is not shim-wrapped (_check_codex_mcp_shim_seed, entrypoint.sh:190-261) - the gate matches by name-prefix/adapter-label, so a user stdio entry named innocuously with command ["codex","mcp-server"] renders RAW (unshimmed, without the cwd-scope/git/depth/kernel-injection functional guards). Per the 08-08 container-boundary ruling this is NOT a security escape: the container is the boundary, an unshimmed in-container codex cannot reach outside the project, and shim-wrap is P2 functional integrity + defense-in-depth (section 2 b1 / section 5), not a broken invariant needing an argv-resolution fix. codex refuses to start on a present-but-unparseable-or-writable hooks.json, or a missing/unparseable profile (_codex_profile_preflight, entrypoint.sh:338-379) - [LIVE GAP in landed code] two preflight gaps remain: profile WRITABILITY is never checked (only existence/non-empty/TOML-parse, entrypoint.sh:340-361), and a MISSING or EMPTY hooks.json is silently ACCEPTED (the writability/parse checks run only inside the [ -e ] && [ -s ] guard, entrypoint.sh:363), so these are open hardening items, not the closed boot gate implied;
- tmux session wrapper: _multiplex_run (entrypoint.sh:308-336) wraps ANY engine; the safeguard watchdog and limit autoresume ride on it;
- host/hub side: cbox dispatcher, session store, bridge, watchdog reconciler.

P2 MEDIATED TOOLS (engine-agnostic through the one cross-engine STANDARD, MCP): cbox-owned MCP servers are enforcement points that work identically for every caller. This is already real, not aspirational: codex_mcp_shim.py enforces cwd-scope + git-worktree (check_cwd_scope_and_git :176-208, deny + audit), rejects caller-supplied instruction keys (:428-432, :440-444), pins model/effort (:433, :445), pins approval-policy=never + sandbox=danger-full-access in-container (:446-448), injects the conduct kernel as developer-instructions into EVERY delegate call (:449), sets child depth env (:375-378) and disables the tool entirely at depth (:502). hermes_delegate_mcp.py enforces depth (:67-68, :676-679), a flock concurrency semaphore (:103-145), and audits (:188). Whoever the caller is - claude, hermes, a future client - these guards hold. The claude-side codex_mode_guard PreToolUse hook is defense-in-depth on top, not the enforcement.

P3 ENGINE-NATIVE (per-engine, never unifiable as a mechanism - only as a source):
- context channels: claude CLAUDE.md kernel block + SessionStart hook payloads; codex AGENTS.override.md render (gen_codex_agents_into, generators.sh:2640-2724, 64000-byte cap) + hooks.json SessionStart; hermes ephemeral system prompt composed in the entrypoint (:591-615, user-policy cap 16384);
- native hook engines: claude settings.json hooks (rich event set, deny-union, managed-settings :ro mirror for four guards); codex hooks.json (today rendered with SessionStart only, gen_codex_hooks_json_into generators.sh:2562-2585; upstream experimental per-tool events per the correction above); hermes config.yaml shell hooks (upstream, unverified in pin);
- native config formats: settings.json / config.toml / config.yaml - and their write idioms (merge_settings_json union, marker blocks, block-replace _hermes_apply_mcp_servers entrypoint.sh:461-523).

The honest theorem of the design: P1 and P2 are engine-agnostic BY CONSTRUCTION; P3 is engine-specific FOREVER. Therefore: hard invariants go to P1/P2 only; text and behavioral features have one SOURCE and N P3 transports; and the unification you can prove is "one source + N adapters + digest parity," never "one mechanism."

2. CAPABILITY ABSTRACTION

A capability is: id, class, enforcement, payload source(s), audience, per-engine binding, degradation policy. The classes, with the census of what exists today:

(a) CONTEXT - passive rules text. Sources: etc/hooks/conduct-kernel.txt (audience: everyone including delegates), etc/hooks/session-core.txt (audience: main drivers only), etc/hooks/orchestrator-global.txt, user policies. Engine-agnostic as SOURCE; delivery is P3 per engine plus one P2 surprise that already exists: the shim injects the kernel into codex delegate calls (codex_mcp_shim.py:449), and hermes-delegate deliberately does not (--ignore-rules, :594 - owner decision 3 of USER_EXTENSION_DESIGN).
(b) GUARDS - active blocking. Two sub-classes with different physics:
  (b1) HARD invariants (must never be lost): delegation depth, cwd scope, codex's own sandbox/approval-policy pin, hooks-config tamper resistance, egress policy. ALL of these are already enforced in P1/P2 (census above). None of them depends on a client hook. This is the red line satisfied by construction, and the design makes it law: a b1 guard MAY have a P3 hook as defense-in-depth but MUST have its enforcement in P1/P2, or the dependent feature does not render for that engine. (Shim-wrap of codex servers is NOT a b1 security invariant per the 08-08 container-boundary ruling - the container is the boundary, so an unshimmed in-container codex is a functional-integrity concern (missing tier pin / depth / kernel injection), not a sandbox escape. It lives in P2 as functional integrity + defense-in-depth, name-labeled entries enforced by the boot gate; a raw user entry is a UX/integrity note, not a red-line violation.)
  (b2) BEHAVIORAL guards (protect the human's conventions, not the sandbox): commit_guard, rm_glob_guard, code_hygiene_guard, agent_label_guard, spawn_gate, claude permission_mode/plan-mode gating (codex_mode_guard.py) - today claude-only via settings.merge.json PreToolUse (six matchers) with four mirrored into managed-settings.merge.json (:ro); permission_mode/plan-mode is claude CLI session state, does NOT propagate to hermes/bare-codex callers. These CAN be lost per-engine without a security hole; losing them silently is still forbidden (degradation policy below).
(c) OBSERVABILITY - PostToolUse enrichment and mechanical logging: continuity_commit_log (Bash), continuity_ledger_sweep (Edit|Write), continuity_session_digest (SessionEnd), devel_explain.py (the DEVEL explainer; [LIVE GAP in landed code, corrected anchor] NOT present in etc/hooks - only a stale __pycache__ artifact remains there; the source is an uncommitted scratchpad draft per PROGRESS_2026_08_07.md, wired nowhere - not in settings.merge.json, not in gen_hooks_dir - i.e. pre-wave, not mid-wave). Claude-native today. The engine-agnostic FLOOR for this class already exists and is load-bearing: post-hoc mining of the engine's own history store via cbox_session_bridge.py (claude-jsonl, codex-jsonl, hermes-sqlite - engines.json history_read). Inline-vs-mined is a fidelity difference, not a feature difference.
(d) TOOLS/MCP - delegates and utility tools. Already the most unified layer in the tree: delegates.json entries with `_cbox {adapter, available_to, backend, side_effects, enabled_when_env}` rendered through one chokepoint _cbox_render_mcp_for_target (generators.sh:254; call sites :2126 hermes, :2193/:2300 claude, :2476 codex) into three native formats. This layer is the TEMPLATE the rest of the design generalizes.
(e) LIFECYCLE - session multiplex, safeguard autoconfirm, limit autoresume, scope farm, login bridges, cross-engine handoff. Mostly P1 (tmux wrapper, entrypoint, watchdog) = engine-agnostic by construction; per-engine facts are already declared in engines.json (resume_argv, preassign_id, login, probe, history_read, seed_channel).

Per-class verdict on "realne engine-agnosticke?": (d) yes at the protocol level (MCP is the only standard all clients speak); (e) yes at the process level (tmux/entrypoint see every engine the same); (b1) yes because it never needed the engine; (a) source yes / transport no; (b2) no - native hook dialects, forever per-engine, adapter-normalized at best; (c) inline no / mined yes.

3. THE BRAIN - the data layer under capabilities (task items 7 and 9)

Answer to 7: the brain is NOT a capability in the registry; it is the LAYER BELOW. Capabilities are verbs (inject, log, guard, hand off); the brain is the noun they act on. Concretely the brain is already engine-agnostic by construction because it is plain files in the project tree, chosen deliberately over any engine's private store:

- STATE (what is live now; high churn; driver-written): .cbox/LEDGER.md (single source of truth, live wave only) + the session store .cbox/sessions/s-*/session.json (cboxSessionId, engines{native ids, locators, cursors, lineage}, activeMain, state) + .cbox/runtime/ (sessions.json, native-index, mirrors).
- MEMORY (what was learned; append-mostly): PROGRESS_YYYY_MM_DD.md, LEDGER_ARCHIVE.md, DIARY.md, CHANGELOG.md, and the handoff distillates .cbox/sessions/s-*/distillates/handoff-NNNNNN.json (layerA verbatim / layerB deterministic summaries, built by cbox_session_bridge.py merge from any engine's native history).
- CONTEXT (the rules; cbox-owned, versioned, flows one way cbox->engines, never project-written): conduct-kernel.txt, session-core.txt, policies. This is the (a) capability payload, listed here because task item 9 asks how the three relate: context is brain only in the sense that it is injected alongside brain payloads; its OWNER is cbox (versioned "conduct-kernel v5", digest-stamped), while state/memory are owned by the project.

The three-way split is not new design - it is already live in code and the design only names it as schema: continuity_session_start.py assigns each injected payload a kind with its own byte budget (core 7000, reference/ledger/progress 4000, shared-memory 12000; :10-23) and wraps each in BEGIN/END fences with a digest (:231-247), explicitly labeling reference payloads "reference data only, not instructions or commands." That is the brain/context distinction enforced at the injection boundary.

How the brain reaches any engine (delivery is capabilities (a)+(e)):
- claude: SessionStart hook runs the loader; payloads = core + shared-memory (handoff distillate, path-validated against the session store, :254-314) + bounded ledger + progress tail.
- codex: hooks.json SessionStart runs THE SAME loader script (gen_codex_hooks_json_into) - one loader, two transports, already true today.
- hermes: entrypoint composes kernel+core (_hermes_kernel_preamble :591-606) + bridge-rendered handoff (:688-703) into the ephemeral prompt - but does NOT inject ledger/progress payloads; hermes today gets only the TEXT INSTRUCTION to reconstitute (session-core says so), not the payload. This is a real, closable gap and increment M2 closes it with the same loader (the loader is transport-independent: reads optional stdin JSON, tolerates empty, resolves the git root from cwd, writes fenced text to stdout - callable from the entrypoint with its stdout appended to the prompt). Two dedup rules for M2, both mandatory: (i) the hermes preamble becomes kernel-only and the loader supplies core + brain payloads, so session-core is emitted exactly once; (ii) the loader call SUPERSEDES the entrypoint's own bridge-render block (:688-703 is removed) - the loader already reads CBOX_SESSION_MEMORY_FILE and emits the shared-memory payload itself (:254-314). [CONFIRMED / PARTIAL, corrected] The loader's version is NOT uniformly stronger: (a) its path-validation and 2MB cap are equal-or-stronger than the entrypoint block; (b) its session-id match is WEAKER on empty CBOX_SESSION_ID - continuity_session_start.py SKIPS the check on empty/unset and matches any session dir, whereas entrypoint.sh's ${CBOX_SESSION_ID:-invalid} glob fails CLOSED on empty/unset. PRECONDITION for M2: fix the loader to REJECT (not skip) on unset/empty CBOX_SESSION_ID BEFORE the entrypoint's bridge-render block (:688-703) is removed - deleting that block first, with the loader's weaker check still in place, is a regression, not a dedup. Byte-retention reconciliation is also required: the loader keeps the FIRST 12000 bytes (prefix) of the distillate, the bridge keeps the LAST 16000 bytes (tail); standardize on the bridge's tail-biased 16000 (recency is the more useful retention direction for a resumed session), update SHARED_MEMORY_BODY_BYTE_CAP and the _bound_payload truncation direction to match, or explicitly document shared-memory as exempt from the generic prefix-cap convention if the two are kept deliberately different.
- any future client: whatever its start-text channel is, the adapter calls the same loader and pipes the output into it. The loader is the brain's one renderer; transports multiply, the renderer does not.

4. WRITE DISCIPLINE - who writes the brain when everything is engine-agnostic (task items 8 and 10)

The existing invariant scales cleanly once it is stated as ROLES, not engines. Three writer roles, all already present in the tree:

- w1 DRIVER (free-form writes to the .md brain): exactly one human-driven session at a time, whichever engine it runs on. ONE-ACTIVE-WRITER is an invariant, not a lock (session-core.txt states "do not add file locking" - ruling). The session store already models it: session.json activeMain/state; the hub (tmux owner per the 07-31 ruling "conduct jadro = cbox hub") is the natural place to KNOW who the active driver is. [LIVE GAP in landed code] Doctor does NOT yet surface a multi-activeMain violation as a row: the current lease check (cbox-session.sh:624, _cbox_session_field_get) checks activeMain only WITHIN a single session id; two different live session ids in the same project scope can each independently hold activeMain with no cross-sid detection anywhere in the tree today. See the M1-addendum in section 8 for the required scan.
- w2 CBOX MECHANICS (deterministic, provenance-stamped, bounded appends): the continuity hooks already write CHANGELOG/PROGRESS mechanically on claude (commit log, ledger sweep, session digest); the bridge writes session.json and distillates; the watchdog writes runtime state. These are infrastructure writers, NOT delegates, and they are allowed because their writes are mechanical (derived from observed events, no judgment). Per-engine today: claude full (PostToolUse+SessionEnd), codex none (SessionStart only - the policy text "without hooks, keep the old manual discipline" covers it), hermes none. Degradation for w2 is class (c): where inline hooks are missing, records can be MINED post-hoc from the native history store by the bridge - but [CONFIRMED, partial] mined records today are chat text ONLY (user/assistant turns); none of the extractors (extract_claude/extract_codex/extract_hermes in cbox_session_bridge.py) capture commit/edit/tool-outcome events. This is lower FIDELITY, NOT equal durability. Hermes is further constrained: its sqlite messages table (id/role/content/timestamp) has no event-type column, so those events may be unminable at the source even if the extractor were extended.
- w3 DELEGATES AND TOOLS: never write the brain. Return a distillate; the driver decides durability. [LIVE GAP in landed code, corrected strength] Enforced today by text (kernel DELEGATE WRITE BOUNDARY) ONLY; for hermes-delegate the ephemeral HOME/cwd (tempfile.mkdtemp + --ignore-rules, hermes_delegate_mcp.py:568-601) is a default-location convenience, NOT a write barrier - PATH is inherited and absolute-path writes to the real project tree are not blocked. This is text-governed like engine-native subagents, and belongs in section 10 (WHAT CANNOT BE UNIFIED), not implied as substrate-enforced.

Per-file-class write matrix (the sharpening the scale-out needs):
- LEDGER.md, OPEN_QUESTIONS.md, DIARY.md: w1 only.
- PROGRESS_*.md, CHANGELOG.md: w1 free-form + w2 mechanical appends (both already true).
- .cbox/sessions/**, .cbox/runtime/**: w2 ONLY (hub/bridge/watchdog owned, JSON, O_NOFOLLOW discipline) - this store is OUTSIDE the ONE-ACTIVE-WRITER invariant, which governs the human-facing .md brain, not the machine store.
- generated context (kernel/core renders): cbox regen only; never written from inside a session.

Task item 10, non-conversational tools: their contract is READ freely (the brain is plain files; a script or MCP server reads LEDGER.md or calls `cbox_session_bridge.py render` like the entrypoint does), WRITE only as w2 if the tool is cbox infrastructure (then it must stamp provenance and be append-bounded), else w3 (return data, do not write). Concurrency answer for w1 stays social/structural (one human, hub-visible); for w2 the writers are single-purpose per file (commit log appends CHANGELOG, bridge owns sessions/) so there is no cross-writer contention by design; where a mediated funnel would help future tooling, `cbox brain append` as a CLI verb is listed as an owner decision (a funnel with provenance, compatible with the no-locking ruling, NOT a lock).

5. CAPABILITY REGISTRY - one declarative source of truth + proof

File: etc/capabilities/capabilities.json + a validator etc/capabilities/capability_registry.py in the exact idiom of engines_registry.py (load/validate/names/get, RegistryError, closed key sets). Schema:

{
  "schema": 1,
  "capabilities": {
    "<id>": {
      "class": "context|guard|observe|tool|lifecycle",
      "enforcement": "hard|behavioral|advisory",
      "audience": "all|driver|delegate",
      "sources": ["etc/hooks/conduct-kernel.txt", ...],
      "enabled_when_env": ["CBOX_...", ...] | null,    // referential, AND-of-nonempty: knob(s) LIVE in etc/registry/settings.json, never duplicated here.
                                                        // list form covers compound prerequisites in scope for M1, e.g. local-qwen needs
                                                        // [CBOX_LOCAL_MODEL_URL, CBOX_LOCAL_MODEL_NAME], hermes-local needs the enable flag
                                                        // AND provider AND (base_url when provider==local). The provider==local-implies-
                                                        // base_url conditional is a richer predicate than flat AND and stays a runtime
                                                        // check in hermes_delegate_mcp.py _apply_config - documented here, not silently
                                                        // assumed to be covered by the compound gate.
      "planes": ["substrate", "mediated", "native"],  // where enforcement/delivery actually happens
      "bindings": {
        "claude":  {"mechanism": "settings-hook:PreToolUse", "matcher": "Bash", "artifact": "...", "status": "live"},
        "codex":   {"mechanism": "hooks-json:PreToolUse", "status": "gated:codex-hooks-experiment"},
        "hermes":  {"mechanism": "config-shell-hook:pre_tool_call", "status": "gated:hermes-hooks-experiment"}
      },
      "degrade_floor": "advisory-text|mine-history|feature-off|block-start|none"
    }
  }
}

Degradation precedence, stated once: `degrade_floor` is capability-level (what an engine WITHOUT a live binding gets); a per-engine binding `status` overrides the floor for that engine only. There is no second per-binding degrade field - one knob, one override.

Ownership boundaries against registry proliferation (the conf_save-vs-whitelist lesson, stated as law):
- settings.json owns KNOBS (what the user can set). capabilities.json references them by env name(s), plural - list form for compound prerequisites (`enabled_when_env` - precedent already in delegates.json).
- engines.json owns ENGINE FACTS (probe, versions, channels). capabilities.json references engines by name; a binding for an engine not in engines.json is a validation error. Precedence stated once, here, for the whole design: engines.json is the SOLE owner of engine facts; the adapter's describe() (section 6) is a validation/consumption function that reads and cross-checks engines.json, never a second source that independently declares a fact engines.json lacks. If describe() would need a field with no engines.json counterpart, that is a registry gap to fix in engines.json, not a second source of truth.
- delegates.json owns TOOL ENTRIES (it IS the class-(d) sub-registry; each entry maps to exactly one tool-class capability; `available_to` remains where it is).
- capabilities.json owns the FEATURE-to-DELIVERY mapping and nothing else.
Referential integrity is a test, not a convention: a new lib/test_capabilities_registry.sh asserts (1) every binding engine exists in engines.json, (2) every enabled_when_env entry - iterating the list/compound form - exists in settings.json variables, (3) every delegates.json entry has its tool capability, (4) every hook command in settings.merge.json / managed-settings.merge.json / generated codex hooks.json / rendered hermes hooks block appears in some capability binding, (5) every hardcoded dispatch site in the section-6 inventory matches its APPLICABLE engine subset - PROJECTION-AWARE, not a flat exact-mirror. The projections are encoded AS DATA - one (file, locator-pattern, predicate) tuple per site, the predicate derived from an engines.json field, sites located STRUCTURALLY by pattern not by line number (line numbers drift; the CLI wave rewrites the same neighborhood). The predicates (alien F4, corrected against engines_registry.py:15,:78-81 - there is NO "none" enum value; a null-history engine has history_read null or the key omitted): HISTORY sites (bridge extract, bridge discover dispatch, resume case, HISTORY_READ_VALUES) project on history_read PRESENT-AND-NON-NULL; RESUME sites project on resume_argv PRESENT-AND-NON-NULL; INSTALLER/COMPOSE and the per-tool bins loops project on install==bins-volume; ADMISSION/DISPATCH-validate/CLI-choices project on the full engine set. So an ad-hoc, registry-invisible delivery, or a new engine missing one of the bounded core edits it is subject to, FAILS CI, while an engine legitimately absent from a projected site (a null-history engine not in the extract/discover dispatch, a non-bins engine not in the bins loops) does NOT false-fail. A null-history fixture engine MUST pass the projected sites (negative fixture, mandatory - a projection that has never been observed to spare a legitimately-absent engine is untested). That last assertion is what makes the registry the single source of truth mechanically rather than by discipline.

Assertion-4 mechanics (alien F8, so the test is not itself silently green): settings.merge.json / managed-settings.merge.json hook commands carry the @HOME@ placeholder, so the binding-vs-command match must NORMALIZE placeholders before comparing (not a raw string equality). The "generated codex hooks.json" and "rendered hermes hooks block" must be produced INTO A SCRATCH DIR by the test itself, never read from a live generated/ tree (tests-lightweight, no dependence on a prior regen). The hermes hooks block is vacuous until M4 lands it, so assertion 4 covers the hermes surface only once that binding exists - until then it is claude + generated-codex only.

Parity proof: generalize the existing context manifest into a capability manifest. Today _cbox_context_manifest_write digests 8 fixed artifacts (kernel, core, claude_md source, loader, codex agents render, shim, settings merge, hermes entrypoint) with a doctor row. The generalization: regen_all (generators.sh:2966) walks capabilities.json and emits generated/capability-manifest.json = {capability x engine -> {mechanism, artifact digest, status}}; _cbox_context_manifest_verify becomes the per-binding drift check; doctor renders the matrix (section 7) from this file. The digest vocabulary and the verify loop already exist - this is a widening, not a new mechanism. The existing context-manifest keys stay as-is during migration (M1 emits both; M3 folds).

Shim-wrap note (NOT a required fix, per the 08-08 container-boundary ruling): the boot gate matches codex servers by name/label (entrypoint.sh:229), so a raw user stdio entry with command ["codex","mcp-server"] renders unshimmed (render_mcp.py rejects only by name, :259-263). This is deliberately NOT closed with an argv-resolution reject: the container is the security boundary, an unshimmed in-container codex cannot escape the project, and danger-full-access is the intended in-container default. The only loss is P2 functional integrity (tier pin / depth / kernel injection) for that one user-authored entry - a UX/integrity note the capability manifest records as a defense-in-depth binding, not a security invariant with a pending code change.

6. ADAPTER CONTRACT - thin per-engine transports

An adapter is one Python module per engine (python-first per the 08-07 ruling; generators.sh call sites keep calling during migration), etc/adapters/<engine>.py, with this fixed surface:

- describe() -> the engine's CHANNEL FACTS, validated against engines.json (engines.json is the sole owner of engine facts, per section 5; describe() never independently declares a fact engines.json lacks): which context channel (seed_channel enum already in engines.json: sessionstart-hook | pointer-prompt | ephemeral-system-prompt), byte budgets (claude payload caps; codex 64000 AGENTS cap; hermes 16384 user-policy cap), hook dialect if any (event names, payload shape, block mechanism, crash semantics fail-open/fail-safe, layering union/replace, trust model), config write idiom (merge-union / marker-block / block-replace / full-file).
- render_context(sources, audience, profile) -> engine-native artifact(s). Claude: CLAUDE.md kernel block + settings hooks entry for the loader. Codex: AGENTS.override.md + hooks.json SessionStart. Hermes: kernel-only preamble + entrypoint loader call.
- render_tools(delegates, target) -> native MCP config. Already exists as _cbox_render_mcp_for_target target branches; the adapter absorbs its per-engine arm.
- render_guards(capabilities) -> native hook config for b2 guards the engine supports, or a DECLINE with reason (feeds the manifest status). MANDATORY: golden payload fixtures per (guard x dialect) - a guard is only "live" on an engine when a recorded native payload fixture drives the actual guard script to a DENY in tests (lib/test_rm_glob_guard.py idiom, extended per dialect). A dialect mismatch otherwise means the guard silently never fires - fail-open disaster with a green render.
- lifecycle facts: resume argv, probe, login - already in engines.json; the adapter only reads them.

Adapter LAW (trust symmetry, from the two existing precedents): any engine flag that bypasses that engine's own hook-trust/consent (codex --dangerously-bypass-hook-trust entrypoint.sh:645; hermes hooks_auto_accept / HERMES_ACCEPT_HOOKS) is allowed ONLY together with an unwritable managed hook config (the codex preflight refuses a writable hooks.json, entrypoint.sh:373-376; the hermes adapter must implement the same refusal for its rendered hooks block + referenced scripts under a :ro mount, because the consent allowlist keys on command strings and would otherwise be a self-extension hole for the in-container agent).

Adding a NEW client = one engines.json entry + one adapter module + registry bindings, plus bounded, enumerated core edits (an admission/dispatch CHECKLIST, not a logic fork). The inventory below is the enumerated set, grouped by projection - each group applies only to engines of a given class, which is why the CI check (section 5, assertion 5) must be PROJECTION-AWARE, not a flat exact-mirror of every list against every engine. Line numbers are illustrative anchors ONLY; the CI test locates each site by a structural pattern, since the CLI wave and future edits shift these numbers.
- ADMISSION (unconditional, every engine): render_mcp.py TARGETS tuple, cbox-session.sh admission case, cbox-session-entry.py ENGINE_RE, cbox_session_bridge.py engine validation.
- ENTRYPOINT DISPATCH (unconditional, every engine - alien F5, was MISSING): entrypoint.sh:619-712 per-engine launch case; a fourth engine with no arm falls through to bare _run_as_user "$@" at :714 (no preflight, no multiplex, no prompt injection). This is the most load-bearing missing site - a CI-green fourth engine would still be unlaunchable without it.
- HISTORY (projected: history_read PRESENT-AND-NON-NULL): cbox_session_bridge.py extract() dispatch, cbox_session_bridge.py discover() dispatch dict + default list (alien F4: discover is history-projected, NOT unconditional - a null-history engine has nothing to discover), engines_registry.py:15 HISTORY_READ_VALUES (further conditional: only if the new engine needs a NEW history format).
- RESUME (projected: resume_argv PRESENT-AND-NON-NULL): cbox-session.sh resume/preassign case.
- CLI SURFACE (unconditional): cbox_session_bridge.py argparse --engine choices (three subcommands).
- INSTALLER / COMPOSE + BINS LOOPS (projected: install==bins-volume): generators.sh _cbox_bins_volume case, cbox-setup.sh channel install list, compose bins-volume generation, cbox _bins_run_install and _bins_channel (return 1 for non-bins), cbox:1939 per-tool bins loop (alien F4: bins-projected via _bins_channel, NOT unconditional - a non-bins engine is skipped by `|| continue`, no crash).
- PER-TOOL LOOPS (unconditional): cbox:3828 `for tool in claude codex hermes`.
- HUB / DOCTOR (unconditional; normally registry-driven, watch for stale fallbacks): cbox hermes/engine doctor rows, cbox_hub.py:36,39 engine fallback list (should stay registry-driven via engines_from_registry, not a stale 2-3 engine literal).
(Cosmetic engine-named strings - render_mcp.py usage line, cbox-setup.sh doc note - are NOT dispatch sites and need no CI coverage.) COMPLETENESS LIMIT (alien F5): assertion 5 enforces only enumerated sites; it cannot by itself catch an un-inventoried one. So M1 ALSO ships a repo-wide three-name pattern-scan tripwire (grep for co-occurrences of claude/codex/hermes as literals outside the inventoried sites) - anything it surfaces is either added to the inventory or explained. If that tripwire is not built, the "ad-hoc delivery FAILS CI" claim is scoped to the assertion-4 hook-command surfaces only, not to all dispatch. The bridge's own discover/extract pair (~60 lines) is genuinely new per-engine code; a null-history engine is legal and simply turns off mining-class capabilities with a doctor row.

7. DEGRADATION - matrix and the red line

The degradation vocabulary is derived from behaviors already in the tree, not invented: block-start (claude refuses to launch on unwrapped codex servers, entrypoint.sh:190-261), enforce-substrate (argv pinning, :ro mounts, boot preflights), enforce-mediated (shim/delegate server-side checks), enforce-native (claude hooks), advisory-text (kernel/core sentences), mine-history (bridge), feature-off (enabled_when_env gates), plus doctor-row visibility for every non-native state. Note: for a capability gated on one var but that actually needs a second var to function (local-qwen, hermes-local), current runtime behavior is NOT feature-off - the tool is advertised as available and the call fails at invocation time. M1 does NOT change render behavior here (alien F2): M1 only DECLARES the compound (list-form) enabled_when_env for the flat-AND cases (local-qwen's URL+NAME) in capabilities.json. It does NOT make the render path consult capabilities.json, and it does NOT yet emit a per-binding doctor WARN when a live binding's env gate is unset - that WARN is a later increment (M3 or a dedicated visibility step), not M1 [correction folded from the M1 code-review: the earlier "surfaces ... as a doctor WARN" overstated M1; the only doctor visibility M1 ships is the capability-manifest row itself]. delegates.json still gates on its single string today, and moving the gate to render-time is a behavior change that lands in M3 (or gets explicit owner sign-off), not M1. The remaining CONDITIONAL case (hermes-local: provider==local implies base_url required) is NOT expressible as a flat AND and is NOT closed by any capabilities gate - it stays a runtime check in hermes_delegate_mcp.py _apply_config; making even that produce feature-off BEFORE the tool is advertised would need a richer conditional-predicate schema or a renderer/doctor operational preflight, out of scope, listed here as a known residual rather than claimed as covered.

Matrix (capability x engine -> mechanism now / floor when absent):

CONTEXT kernel+core
  claude: CLAUDE.md block + SessionStart loader (live) | codex: AGENTS.override + SessionStart loader (live) | hermes: ephemeral prompt (live) | future: any start-text channel (admission requirement) - floor: none needed; this capability is the floor.
CONTEXT to delegates
  codex delegates: shim developer-instructions injection (live, P2) | hermes-delegate: deliberately none (--ignore-rules; owner decision stands) | engine-native subagents: claudeMd multiplication (claude) / nothing (codex, hermes) - named non-unifiable.
GUARD b1 (depth, scope, mode, tamper, egress)
  ALL engines: enforce-substrate + enforce-mediated (live today; the claude codex_mode_guard hook remains defense-in-depth). Floor: none permitted - if a b1 precondition cannot hold for an engine, the DEPENDENT FEATURE does not render for it (feature-off + doctor), never text. This is the red line, and note it is already the tree's behavior, e.g. an engine that cannot be argv-pinned would simply not get codex delegates rendered.
GUARD b2 (commit, rm-glob, hygiene, label, spawn)
  claude: enforce-native, deny-union, 4/6 mirrored in managed :ro (live; the CRITICAL-2 finding of USER_EXTENSION_DESIGN - mirror rm_glob+spawn_gate too - stands) | codex: gated experiment (Bash-only coverage even if verified: commit_guard+rm_glob map, code_hygiene does NOT - codex file edits bypass a Bash-only PreToolUse; label/spawn have no codex analog) | hermes: gated experiment (shell hooks pre_tool_call, matcher on terminal tool; fail-open on crash) | floor for all: advisory-text (the kernel DESTRUCTIVE COMMANDS paragraph is exactly this) + doctor WARN row "guard X: advisory-only on engine Y". Silent loss becomes structurally harder once M1+M3 land and the manifest carries per-binding status with doctor rendering it - today that is a design commitment, not current behavior. This scopes to b2 GUARD delivery only; it does NOT cover the writer-identity/activeMain channel (section 4), a distinct silent-loss channel with zero code today (see section 4/8 multi-activeMain gap).
OBSERVE (commit log, ledger sweep, session digest, devel-explain)
  claude: enforce-native PostToolUse/SessionEnd (live) | codex: mine-history floor now (bridge codex-jsonl); native PostToolUse observe-only later if the experiment passes | hermes: mine-history floor (state.db) or post_tool_call shell hook after the gate | devel-explain: inline is claude-only UX; the engine-agnostic form of the same knowledge is the kernel ENVIRONMENT HONESTY text (already delivered to all three).
TOOLS/MCP
  all: one chokepoint render into native formats (live). Future client without MCP support: tools feature-off; delegates unreachable; doctor row.
LIFECYCLE
  multiplex/watchdog/autoconfirm: P1 tmux, engine-agnostic (live; autoresume/safeguard currently claude-scoped in the entrypoint condition - a registry binding, not a physics limit) | resume: per-engine argv declared in engines.json (live) | handoff: bridge + distillates, all three (live).
BRAIN inject
  claude/codex: loader (live) | hermes: kernel+handoff live, ledger/progress gap -> M2 closes with the same loader | future: adapter calls loader.
BRAIN write
  w1 any driver engine (text-governed) | w2 claude-native now, mine-history floor for codex/hermes (M5 makes the bridge able to backfill PROGRESS/CHANGELOG mechanical records post-session) | w3 never, text-governed.

8. MIGRATION - each increment leaves a functional tree (cbox publishes by rsync of disk state)

Sequencing constraint first: the CLI-unification wave (funded, ordered watchdog -> CLI -> user-layer inc3-5) rewrites setup.sh into `cbox setup` and touches the same generator call sites. M1 is safe to run in parallel (pure additive files + tests); M2+ sequence AFTER the CLI wave lands to avoid rebasing adapters onto a moving dispatcher.

M1 REGISTRY CENSUS (no behavior change): write capabilities.json describing ONLY what exists today (every current hook, render, gate above), capability_registry.py validator (mirroring engines_registry.py exactly: load/validate/names/get CLI, RegistryError(ValueError), closed key sets, per-class key-set validation), referential-integrity test (section 5), and the widened capability-manifest emitted ALONGSIDE the existing context-manifest as a SEPARATE file generated/capability-manifest.json (M1 emits both; M3 folds). No behavior change means NONE (alien F2/F6): (a) NO die-on-drift verify for the new manifest in M1 - _cbox_context_manifest_verify dies and is called from the setup flow (cbox:3419), so mirroring that would let a stale capabilities.json block setup; verify semantics land with M3. (b) The capability-manifest is emitted LAST in regen_all (after the existing manifest write near generators.sh:3003), never gating anything. (c) The doctor row is ONE literal name declared in doctor_extra_rows (etc/registry/settings.json, where "context-manifest"/"sessions" already sit) - the doctor census guard (cbox:3880-3900) is bidirectional AND matches literal quoted strings, so a dynamic per-capability row name breaks it BOTH directions; use one fixed literal, not per-capability rows. M1 necessarily edits three live files (generators.sh new gen fn + regen_all call; cbox doctor row; settings.json doctor_extra_rows entry) - additive in BEHAVIOR if these three traps are avoided. Value: the drift trap closes first (the census test fails on future ad-hoc delivery), and the matrix makes today's asymmetries visible to Marek before anything moves.
M1-ADDENDUM MULTI-ACTIVEMAIN SCAN (closes the section 4 [LIVE GAP in landed code]): doctor scans all .cbox/sessions/s-*/session.json under the workspace root for activeMain+state, reads pid/procStart from .cbox/runtime/sessions.json (alien F3 CORRECTED DATA LOCATION: pid/procStart live in the runtime leg map written by _cbox_runtime_leg_write, cbox-session.sh:254-278, read via :629 - NOT in session.json, which holds only activeMain/state at :342/:624; the earlier "session.json ... activeMain+pid+procStart" spec would make the WARN never fire), applies the existing _cbox_proc_live liveness check per entry, and WARNs if more than one is simultaneously live ("multiple live session mains in one project scope"). To avoid a second doctor census edit, prefer REUSING the already-declared "sessions" doctor row for this WARN rather than adding a new row. MANDATORY negative fixture: a two-live-mains fixture (two session dirs both activeMain, both with a live pid/procStart in the runtime map) that MUST produce the WARN - a scan that has never been observed to fire is untested. This is a distinct silent-loss channel from the b2 guard-delivery one above: a writer-identity gap, not a guard-delivery gap, not covered by the manifest/doctor work by itself.
M2 LOADER UNIFICATION (first real delta): PRECONDITION (section 3) - fix the loader to REJECT on unset/empty CBOX_SESSION_ID before proceeding. Then: the hermes entrypoint calls continuity_session_start.py (as user, cwd=workspace, stdin `< /dev/null` - _read_stdin_payload tolerates empty input - stdout appended to the ephemeral prompt after the kernel); _hermes_kernel_preamble drops session-core; the entrypoint's own bridge-render block (:688-703) is REMOVED because the loader supersedes it (dedup rules, section 3). Result: one brain renderer, three transports; hermes stops being the engine that is only told to read the ledger. Test: extend test_continuity_session_start/test_kernel_lang with a hermes-transport fixture, plus a fixture-test requirement covering (a) empty-session-id reject (the precondition above), and (b) loader/bridge byte-cap and truncation-direction parity for the same distillate (section 3 byte-retention reconciliation).
M3 ADAPTER EXTRACTION (mechanical, python-first): move the per-engine arms of _cbox_render_mcp_for_target, gen_codex_agents_into, gen_codex_hooks_json_into, the claude settings merge, and the hermes render into etc/adapters/{claude,codex,hermes}.py behind the section-6 contract; generators.sh keeps its function names as thin shims (the 11-test awk-parse surface from the CLI design note applies here too - repoint in the same increment). Acceptance gate = a frozen pre-M3 oracle, not the existing context-manifest mechanism: before extraction, render every artifact the moved functions produce (_cbox_render_mcp_for_target 4 call sites - generators.sh:2126 hermes, :2193/:2300 claude, :2476 codex, per alien F7 the earlier "x5" was wrong - gen_codex_agents_into, gen_codex_hooks_json_into, claude settings merge, hermes render) from a fixed pinned fixture set, and store the digests OUTSIDE generated/ so a later regen cannot overwrite them; after extraction, re-render from the identical fixtures and byte-compare every moved artifact against the frozen oracle. The context-manifest mechanism (generators.sh:2820-2900) is NOT this gate - it self-refreshes on every regen and covers only 1 of the ~5 moved surfaces, so it cannot by itself prove pre/post-extraction identity.
M4 GUARD EXPERIMENTS (two independent, gated, host-side): (i) codex - enable [features].codex_hooks in the managed profile on a host branch, record REAL PreToolUse payloads, wire commit_guard+rm_glob behind golden fixtures, prove deny actually blocks, prove union-vs-replace, then flip the binding status to live; keep code_hygiene advisory on codex (coverage physics). (ii) hermes - verify the pinned version has shell hooks, render a cbox `hooks:` block via a block-replace sibling of _hermes_apply_mcp_servers, scripts under a :ro mount, hooks_auto_accept only with the unwritable preflight (adapter law), golden fixtures, prove `{"decision":"block"}` blocks and crash does not. Each lands independently; failure of either changes a matrix cell to its floor, not the architecture. Bump tripwires on both pins.
M5 BRAIN WRITE FLOOR: bridge grows a mine-to-mechanical-records mode (backfill PROGRESS step lines / CHANGELOG commit sections from native history for engines without w2 hooks), provenance-stamped as "mined". Scope note: M5 as specified backfills chat-derived narrative only, until the extractors (and for hermes the underlying history schema itself) are extended to capture commit/edit/tool-outcome events with a dedup key, a cursor, and provenance - that schema/extractor extension is itself unscoped work here, not assumed-covered by M5. Owner-decision-gated: `cbox brain append` funnel verb.
M6 FOURTH-ENGINE PROOF: a stub engine entry + adapter in tests only (test fixture engine with seed_channel pointer-prompt, history_read null / key omitted - NOT the string "none", which is not a valid enum value per engines_registry.py:15,:78-81 and would fail registry validation) proving the admission contract end-to-end: it must receive kernel via its declared channel, tools per available_to, matrix rows render, an entrypoint launch arm exists (section 6 ENTRYPOINT DISPATCH), and the projected CI sites spare it correctly, nothing else breaks. This is the regression harness that keeps "any client" true; its null-history fixture doubles as the mandatory negative fixture for section 5's projection predicates.

9. FUTURE CLIENT - minimal admission contract

MUST (or the client cannot be a cbox engine at all): (1) launchable under the cbox entrypoint with argv fully controlled by cbox (always true - cbox starts it); (2) SOME start-text injection channel - any of: config-imported file, system-prompt env/flag, session-start hook, or worst-case a pointer-prompt (engines.json already reserves this enum value: a one-line pointer typed into the TUI by the tmux wrapper telling the agent to read the rendered context file - the mechanism of last resort, TTY-automation, listed honestly as fragile); (3) tolerance of the tmux wrapper (true for any terminal program).
SHOULD (each absence = declared feature-off + doctor row, not rejection): MCP client support (else no delegates/tools); a discoverable history store with a bridge extract implementation (else no handoff/mining - lifecycle continuity degrades to the .md brain only); a hook dialect (else all b2 guards advisory-text, observe = mine-or-nothing).
The hermes floor is the proof this contract is livable: hermes entered exactly through (1)+(2 ephemeral-prompt)+(3), with tools via MCP and continuity via sqlite mining, and lost nothing silently.

10. WHAT CANNOT BE UNIFIED (the honest list)

- One delivery mechanism. Three context channels with different multiplication semantics (claude CLAUDE.md imports multiply per spawn; codex AGENTS is a single 64000-byte file; hermes is a per-process env prompt capped by prompt budget) and three hook dialects. Unifiable: source, renderer, proof. Not unifiable: the transport.
- In-band interception of engine-NATIVE tools on an engine without a hook API (or outside its coverage: codex non-Bash tools today). Physics: the engine's own file/exec tools run inside its process; without a callback there is no seam. The substrate can constrain WHAT is reachable (mounts, egress, argv), never WHICH native call happens.
- Engine-native subagent governance (agent_label_guard, spawn_gate) on engines whose spawn concept differs or is absent - codex has no Agent tool (its analog is the MCP delegate path, which IS guarded, in P2); hermes subagents fire only observe events upstream.
- Delegate write-boundary (w3) as a mechanical guarantee - text-governed for engine-native subagents (their file tools are legitimate and indistinguishable in P1) AND, per the corrected anchor in section 4, for hermes-delegate too: its ephemeral HOME/cwd is a convenience default, not an enforced write barrier (PATH inherited, absolute-path writes to the real project tree not blocked).
- One writer mechanism for the brain: w1 stays a social invariant by explicit ruling (no locks); the design surfaces violations (hub/doctor) instead of preventing them.
- Live TUI injection: [corrected anchor] cbox ALREADY pushes text via tmux send-keys today (limit_watchdog.py inject(): resume prompt + Enter; safeguard_confirm(): bare Enter) - the claim that text cannot be pushed into a running session is false. The real constraint is that in-band send-keys is an UNTRUSTED best-effort side channel: it races the input loop and cannot carry authenticated startup context. STRUCTURED context (kernel/hooks/MCP defs) remains next-session (re-render + restart) - that stays the change-propagation model for CAPABILITY content; regen + manifest + doctor governs that structured path, not the send-keys side channel.
- The consent/trust models of the three hook engines (claude deny-union + managed :ro, codex trust-bypass flag + writable-preflight, hermes string-keyed consent + auto-accept) - normalized only as the adapter LAW pattern (bypass requires unwritable source), never as one model.

11. OWNER DECISIONS

1. Guard experiments (M4): run both, one, or neither now? Recommended: both, gated, hermes first (its dialect is closer to claude's and its coverage includes the terminal tool; codex is Bash-only AND experimental upstream). Cost of neither: b2 column for codex/hermes stays advisory-text forever - visible, honest, safe, just weaker.
2. `cbox brain append` funnel verb (M5): plain convention (today) vs a provenance-stamping CLI funnel for w2/tools. Recommended: defer until a real non-cbox tool needs it; the taxonomy alone (per-file-class matrix) is enforceable by doctor without new machinery.
3. hermes-delegate kernel: keep --ignore-rules (existing owner decision) even once hermes hooks exist? Recommended: keep; the delegate's guards are P2 server-side already, and injecting the kernel would change delegate behavior globally (the exact reason it was declined before).
4. Pointer-prompt admission floor: accept TTY-automation as a legal last-resort context channel for a hookless future client, or require a real injection channel and reject clients without one? Recommended: keep the enum, mark bindings that use it "fragile" in the manifest, never use it for anything but context.

12. LIVE CHECKS / GATES (all host-side, each with a bump tripwire)

- codex pinned version: hooks feature flag exists? PreToolUse fires on Bash? deny blocks? union or replace? payload shape? (discharges the 07-31 unknown; until then codex b2 = gated).
- hermes pinned version: shell hooks present? VALID_HOOKS set? block JSON honored? crash = allow confirmed? consent bypass semantics? (until then hermes b2 = gated).
- loader-from-entrypoint (M2): confirm the loader's git-root resolution and payload rendering under the hermes launch environment (statically checked in-container; live render is a host run).
- claude deny-union re-verification on CC bump (existing tripwire, now load-bearing for the b2 claude column).
- frozen-oracle byte-compare gate for M3 (see M3), not the context-manifest mechanism.

ADR
- Context: cbox delivers every feature per-engine ad hoc; claude is rich (hooks), codex thinner (SessionStart + experimental upstream hooks), hermes was prompt-only (upstream has since grown shell hooks); Marek ruled the conduct core is the hub managing tmux sessions, delegates are leaves, and asked for one architecture that any future client can join, extended mid-task to unify capability delivery with the shared-brain vision (.cbox/ files + session store + bridge).
- Decision: a three-plane physical model (substrate / mediated tools / engine-native) with the red line as a placement rule (hard invariants only in the two engine-blind planes - which the tree satisfies; shim-wrap is P2 functional integrity + defense-in-depth per the 08-08 container-boundary ruling, NOT a b1 security gap with a pending fix; the codex-preflight items in sections 1/4 remain [LIVE GAP] hardening notes, not red-line breaks); one declarative capability registry with strict ownership boundaries against the three existing registries and mechanical, PROJECTION-AWARE referential-integrity tests (projections encoded as data, predicates from engines.json fields, null-history the correct absence not the string "none"); thin per-engine Python adapters with a fixed contract including golden payload fixtures and the trust-symmetry law; the brain as the data layer below capabilities (state/memory/context split, w1/w2/w3 writer roles, per-file-class write matrix, session store outside ONE-ACTIVE-WRITER); parity proven by widening the existing context-manifest digest into a capability manifest rendered as a doctor matrix; six increments, M1 parallel-safe and strictly no-behavior-change (census + doctor visibility + separate manifest, no die-on-drift, no render-gate move), M2+ after the CLI wave, guard enablement strictly behind host experiments. Passed final alien fable/max review (GREENLIGHT-WITH-CONDITIONS); the F1-F8 conditions are folded in above.
- Consequences: adding a client becomes one registry entry + one adapter + bridge extract (or declared absences), with the stub-engine test keeping that true; b2 GUARD loss becomes structurally harder once M1+M3 land, with manifest status + doctor row generated from the same source that generates the delivery - today a design commitment, not current behavior, and scoped to b2 guard delivery only (the writer-identity/activeMain channel is a distinct silent-loss channel with zero code today, see section 4/8); hermes stops being the under-served engine (loader unification closes the brain-injection gap); the b2 guard columns for codex/hermes become honest declared states (gated -> live or advisory) instead of unstated absences; cost accepted: a fourth registry file (bounded by the referential-integrity tests), and the pointer-prompt floor admits fragile TTY automation as a named last resort.

Key files: /zerolab/agent_ecosystem/cbox/etc/engines/engines.json, /zerolab/agent_ecosystem/cbox/etc/engines/engines_registry.py, /zerolab/agent_ecosystem/cbox/etc/mcp/delegates.json, /zerolab/agent_ecosystem/cbox/etc/mcp/render_mcp.py, /zerolab/agent_ecosystem/cbox/etc/mcp/codex_mcp_shim.py (:176-208, :375-378, :412-450, :502), /zerolab/agent_ecosystem/cbox/etc/mcp/hermes_delegate_mcp.py (:67-68, :103-145, :594, :676-679), /zerolab/agent_ecosystem/cbox/etc/hooks/conduct-kernel.txt, /zerolab/agent_ecosystem/cbox/etc/hooks/session-core.txt, /zerolab/agent_ecosystem/cbox/etc/hooks/continuity_session_start.py, /zerolab/agent_ecosystem/cbox/etc/claude/settings.merge.json, /zerolab/agent_ecosystem/cbox/etc/claude/managed-settings.merge.json, /zerolab/agent_ecosystem/cbox/templates/generators.sh (:254, :2126, :2193, :2300, :2476, :2562-2585, :2640-2724, :2726-2757, :2820-2900, :2966), /zerolab/agent_ecosystem/cbox/entrypoint.sh (:190-261, :308-336, :338-379, :461-523, :525-615, :635-645, :688-711), /zerolab/agent_ecosystem/cbox/lib/cbox_session_bridge.py, /zerolab/agent_ecosystem/cbox/lib/cbox_hub.py, /zerolab/agent_ecosystem/cbox/etc/registry/settings.json, /zerolab/agent_ecosystem/cbox/docs/USER_EXTENSION_DESIGN.md, /zerolab/agent_ecosystem/cbox/docs/CLI_UNIFICATION_DESIGN.md.
