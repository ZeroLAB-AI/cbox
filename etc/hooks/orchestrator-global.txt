ORCHESTRATION: On large/multi-step tasks, act as a lean orchestrator - delegate heavy reading, computation, and auditing to isolated subagents (each with its own context, returning a distillate). Persist state continuously per the durable-continuity policy (./.claude/LEDGER.md + PROGRESS_YYYY_MM_DD.md). Verify adversarially before ACCEPT. Prefer abstaining over confidently wrong. Do small tasks directly. A project-level ORCHESTRATOR.md takes precedence over this text.

AGENTS - ENFORCE ALWAYS: follow the subagent-routing policy (~/.claude/policies) for every spawn - explicit agentType (built-in Explore/Plan exempt), model/effort from the agent definition, spawn label "<agentType> (<model>/<effort>): <task>" (omit /<effort> when the definition sets none), escalation worker -> debugger/code-reviewer -> alien (alien never for routine work).

CODEX: Never delegate automatically - the OpenAI subscription is separate. Propose the delegation and ASK first; after approval call the MCP tool directly (mcp__codex-*__codex) - no relay agents except for parallel fan-outs inside workflows. Set codex parameters from the parent's CURRENT permission mode: auto/bypass -> approval-policy=never; plan -> never + read-only; default/unsure -> on-request. Sandbox inside the cbox container is ALWAYS danger-full-access, even a read-only review (the container is the boundary; bwrap cannot create namespaces there, so read-only and workspace-write both fail the moment codex runs a shell command); the guard gates every such call - including promoted read-only - on cwd/scope/git, so ALWAYS pass an in-scope git-worktree cwd. Host sessions: workspace-write for writes, read-only for reads. In-container plan mode has no working codex path (read-only dies on bwrap, full-access is denied by the plan gate) - defer any codex call until plan mode ends. ALWAYS pass an absolute cwd that is a git work-tree inside the allowed scope (codex_scope config; in the container via the CODEX_GUARD_CONFIG env); the PreToolUse guard blocks non-compliant calls.
